# Make sure that the_flying_circus() returns True
def the_flying_circus():
    if 5==5 and 5<=10 or 0==0:
        return True
        # Start coding here!
        # Don't forget to indent
        # the code inside this block!
    elif 10>10 or 4==3:
        return False
        # Keep going here.
        # You'll want to add the else statement, too!
    else:
        print "invalid"
     
the_flying_circus()

#python uses 'and' 'or' 'not' keyword unlike && || !